import React from "react";
import Tone from "./";
import { shallow } from "enzyme";

it("should render nothing", () => {});

it("should play on mount");

it("should play when switching from play=false to play=true");

it("should stop playing when switching from play=true to play=false");
